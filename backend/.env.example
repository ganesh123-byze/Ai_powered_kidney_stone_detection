# ============================================================================
# Kidney Stone Detection Backend - Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values
# DO NOT commit .env to repository - it contains sensitive information

# ============================================================================
# API Configuration
# ============================================================================
# API host and port
API_HOST=0.0.0.0
API_PORT=8000

# CORS Origins - comma-separated for multiple origins
# Local development
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Production - add your frontend domain
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================================================
# Model Configuration
# ============================================================================
# Model architecture: resnet50 or densenet121
MODEL_ARCHITECTURE=resnet50

# Path to model checkpoint
# Online: can be a URL or local path
# Render: /persistent/kidney_models/best_model.pth
MODEL_PATH=saved_models/best_model.pth

# Model name (for logging)
MODEL_NAME=kidney_stone_detector

# Device: cuda, cpu, or auto
DEVICE=auto

# Use mixed precision for inference (faster but slightly less accurate)
USE_AMP=true

# ============================================================================
# Inference Configuration
# ============================================================================
# Maximum image size for processing (pixels)
MAX_IMAGE_SIZE=512

# Supported image formats (comma-separated)
SUPPORTED_FORMATS=jpg,jpeg,png,bmp

# Batch size for batch predictions
INFERENCE_BATCH_SIZE=4

# ============================================================================
# Logging Configuration
# ============================================================================
# Log level: TRACE, DEBUG, INFO, SUCCESS, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file rotation size (e.g., 10 MB)
LOG_ROTATION=10 MB

# Log retention days
LOG_RETENTION=7

# ============================================================================
# Storage Configuration (for Render persistent storage)
# ============================================================================
# Upload directory for temporary files
UPLOAD_DIR=data/uploads

# Cleanup interval (minutes) - remove old uploads
CLEANUP_INTERVAL=60

# ============================================================================
# Performance Tuning
# ============================================================================
# Number of parallel inference workers
NUM_WORKERS=2

# Cache inference results (in-memory)
ENABLE_CACHE=true

# Cache TTL in seconds
CACHE_TTL=3600

# ============================================================================
# Production Security (Render/Cloud)
# ============================================================================
# API key for protected endpoints (optional)
# API_KEY=your-secret-api-key-here

# Enable HTTPS only
HTTPS_ONLY=false

# Rate limiting (requests per minute)
RATE_LIMIT=100
