services:
  - type: web
    name: kidney-detection-backend
    runtime: python
    plan: free
    
    rootDir: backend
    
    buildCommand: |
      pip install --upgrade pip setuptools wheel && \
      pip install --no-cache-dir -r requirements-render.txt && \
      mkdir -p logs data/uploads saved_models && \
      python download_model.py && \
      chmod +x startup-final.sh
    
    startCommand: ./startup-final.sh
    
    healthCheckPath: /health
    
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: PYTHONDONTWRITEBYTECODE
        value: "1"
      - key: PORT
        value: "8000"
      - key: CORS_ORIGINS
        value: "https://kidney-detection-frontend.onrender.com,https://ai-powered-kidney-stone-detection.vercel.app,http://localhost:3000,http://localhost:5173"

  - type: web
    name: kidney-detection-frontend
    runtime: node
    plan: free
    
    rootDir: frontend
    
    buildCommand: npm install && VITE_API_URL=https://kidney-detection-backend-9n06.onrender.com npm run build
    
    startCommand: npx serve -s dist -l 3000 --no-clipboard --cors --single
    
    envVars:
      - key: VITE_API_URL
        value: "https://kidney-detection-backend-9n06.onrender.com"
      - key: PORT
        value: "3000"
